const checkout = require("../../../lib/solutions/CHK/checkout");

describe.only("checkout tests", () => {
  it("should implement the checkout method", ()=> {
      const result = checkout("SOMETEXT");
  });
});
